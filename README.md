Comandos Git ocupados:

git clone https://github.com/PabloRuizRa/SansaLibros.git

al entrar de nuevo en el bash coloque:

git init
git add .
git commit -m "El mensaje que no recuerdo cual era"
git push -u origin main
